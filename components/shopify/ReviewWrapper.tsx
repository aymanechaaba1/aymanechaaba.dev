import React from 'react';

function ReviewWrapper({ pathRef }: { pathRef: string }) {
  let videoExtensions = ['mp4', 'mov'];
  let imageExtensions = ['webp', 'jpg', 'png'];

  return <div>ReviewWrapper</div>;
}

export default ReviewWrapper;
